#include "caf_error.h"

namespace CAF
{
	/*
	char* getCAFLastError(int32_t errorcode, char* buf, int32_t buf_len)
	{
		if (NULL == buf || -1 == buf_len)
		{
			return NULL;
		}

		return strerror_r(errorcode, buf, buf_len);
	}
	*/
}